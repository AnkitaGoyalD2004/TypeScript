// let myName: string = "Ankita"
let myName : string  = "Ankita";
let meaningOfLife: number;
let isLoading:boolean;
let album: any ; // any is a type it means album can be any data type

myName = "Goyal";
meaningOfLife = 42;
isLoading = true;

// album = "Van Halen"
// album = 34
// album = true

let c : number | string ;// c is of union type 

const sum = (a:number , b:number)=>{
    return a + b;
}

let postId : string | number ;
let isActive:number | boolean;

let re: RegExp = /\w+/g;
